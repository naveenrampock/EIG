import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DicomMoreInfoComponent } from './dicom-more-info.component';

describe('DicomMoreInfoComponent', () => {
  let component: DicomMoreInfoComponent;
  let fixture: ComponentFixture<DicomMoreInfoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DicomMoreInfoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DicomMoreInfoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
