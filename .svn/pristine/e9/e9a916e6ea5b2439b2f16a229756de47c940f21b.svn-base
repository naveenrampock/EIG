import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SharingRecordsComponent } from './sharing-records.component';

describe('SharingRecordsComponent', () => {
  let component: SharingRecordsComponent;
  let fixture: ComponentFixture<SharingRecordsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SharingRecordsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SharingRecordsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
